import styled from "./RightBar.module.css";

const RightBar = ({ userObj }) => {
  return (
    <div className={styled.container}>
      <ul className={styled.rightBar__container}>
        <li>dd</li>
        <li>ff</li>
        <li>cc</li>
      </ul>
    </div>
  );
};

export default RightBar;
